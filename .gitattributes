# LSX Demo Theme - Git Attributes Configuration
# WordPress Block Theme Development - Consistent file handling across platforms
# Ensures proper line endings, encoding, and binary file detection for WordPress development

# =============================================================================
# TEXT FILE DETECTION AND LINE ENDINGS
# =============================================================================

# Auto-detect text files and normalize line endings for cross-platform compatibility
* text=auto                  # Let Git determine text vs binary files automatically

# WordPress development files - Force Unix (LF) line endings for consistency
# This ensures consistent behavior across Windows, macOS, and Linux development environments
*.php text eol=lf            # PHP files (WordPress core standard)
*.js text eol=lf             # JavaScript files (WordPress block development)
*.css text eol=lf            # CSS files (WordPress themes)
*.scss text eol=lf           # Sass files (WordPress build process)
*.json text eol=lf           # JSON configuration files (theme.json, package.json)
*.md text eol=lf             # Markdown documentation files
*.html text eol=lf           # HTML template files (WordPress block templates)
*.txt text eol=lf            # Text files (readme.txt, WordPress requirements)
*.xml text eol=lf            # XML configuration files
*.yml text eol=lf            # YAML configuration files
*.yaml text eol=lf           # YAML configuration files

# =============================================================================
# BINARY FILES - WORDPRESS THEME ASSETS
# =============================================================================

# Mark binary files explicitly to prevent line ending conversion and text processing
# WordPress theme assets should be handled as binary for proper functionality

# Image formats - WordPress media and theme assets
*.png binary                 # PNG images (WordPress featured images, icons)
*.jpg binary                 # JPEG images (WordPress photo content)
*.jpeg binary                # JPEG images (alternative extension)
*.gif binary                 # GIF animations (WordPress media)
*.svg binary                 # SVG graphics (WordPress block icons, theme graphics)

# Web font formats - WordPress theme typography
*.woff binary                # Web Open Font Format (modern browsers)
*.woff2 binary               # WOFF2 compressed fonts (optimal format)
*.ttf binary                 # TrueType fonts (fallback compatibility)

# Additional binary formats for WordPress development
*.ico binary                 # Favicon files
*.pdf binary                 # PDF documents
*.zip binary                 # Archive files (theme packages)
*.tar.gz binary              # Compressed archives
*.webp binary                # Modern image format
*.avif binary                # Next-generation image format
