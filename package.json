{
    "name": "lsx-demo-theme",
    "version": "1.0.0",
    "description": "A modern WordPress block theme with Full Site Editing capabilities",
    "scripts": {
        "build:css": "sass src/scss/style.scss | postcss > style.css",
        "build:editor": "sass src/scss/editor.scss | postcss > assets/css/editor.css",
        "watch:css": "sass --watch src/scss/style.scss | postcss > style.css",
        "start": "wp-scripts start",
        "build": "wp-scripts build && npm run build:css && npm run build:editor",
        "format": "wp-scripts format",
        "lint:css": "wp-scripts lint-style",
        "lint:js": "wp-scripts lint-js",
        "lint:php": "composer run-script lint",
        "test": "playwright test",
        "test:a11y": "playwright test tests/accessibility"
    },
    "devDependencies": {
        "@wordpress/scripts": "^27.0.0",
        "autoprefixer": "^10.4.14",
        "postcss": "^8.4.24",
        "postcss-cli": "^10.1.0",
        "sass": "^1.69.0",
        "@playwright/test": "^1.39.0"
    },
    "browserslist": [
        "extends @wordpress/browserslist-config"
    ],
    "author": "LightSpeed",
    "license": "GPL-2.0-or-later"
}
